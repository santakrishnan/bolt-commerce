/**
 * Shared Services
 *
 * This module contains reusable service classes and factories
 * for API clients, data fetching, caching, etc.
 *
 * Guidelines:
 * - Services should be framework-agnostic where possible
 * - Use dependency injection patterns for testability
 * - Export both the service class and a factory function
 *
 * Example:
 *   export { ApiService, createApiService } from './api-service'
 *   export { CacheService } from './cache-service'
 */

// Export services here as they are added
// export { ApiService, createApiService } from './api-service'

// Placeholder export to make this a valid module
export {};
